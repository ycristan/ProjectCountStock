// ponytail: dead code — session creation now happens via the /admin/sessao/solo wizard.
// Left in place (not deleted) because this session's GitHub MCP tooling has no file-delete
// capability. Safe to `git rm` this file next time someone has local git/GitHub-UI access.
export {}

name: Database security tests

on:
  pull_request:
    branches: [main, codex/team-flow-contracts]
    paths:
      - 'supabase/**'
      - 'tests/http/**'
      - 'actions/**'
      - 'tests/database/**'
      - 'app/**'
      - 'lib/**'
      - 'components/**'
      - '*sentry*'
      - 'instrumentation*'
      - 'next.config.ts'
      - 'package*.json'
      - '.github/workflows/database-security-tests.yml'
  workflow_dispatch:

concurrency:
  group: database-security-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read

jobs:
  database-security:
    name: Apply migrations and verify authorization
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: '2.117.0'

      - name: Initialize disposable local Supabase project
        run: |
          if [ ! -f supabase/config.toml ]; then
            supabase init
          fi

      - name: Start disposable Supabase
        run: supabase start

      - name: Verify migration preserves existing closed counts (disposable database only)
        env:
          PGHOST: 127.0.0.1
          PGPORT: '54322'
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: postgres
        run: |
          test "$GITHUB_ACTIONS" = "true"
          supabase db reset --help
          supabase migration up --help
          supabase db reset --local --version 20260914142358 --no-seed
          psql -v ON_ERROR_STOP=1 -f supabase/tests/migrations/warehouse-before.sql
          supabase migration up --local
          psql -v ON_ERROR_STOP=1 -f supabase/tests/migrations/warehouse-after.sql

      - name: Verify team foundation upgrade (disposable database only)
        env:
          PGHOST: 127.0.0.1
          PGPORT: '54322'
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: postgres
        run: |
          test "$GITHUB_ACTIONS" = "true"
          supabase db reset --local --version 20260917073526 --no-seed
          psql -v ON_ERROR_STOP=1 -f supabase/tests/migrations/warehouse-before.sql
          psql -v ON_ERROR_STOP=1 -f supabase/tests/migrations/team-foundation-before.sql
          supabase migration up --local
          psql -v ON_ERROR_STOP=1 -f supabase/tests/migrations/team-foundation-after.sql

      - name: Run database authorization tests
        run: |
          supabase db reset --local --no-seed
          supabase test db --help
          supabase test db supabase/tests/database

      - name: Lint database functions
        run: supabase db lint --level error

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Concurrent team edits and freeze (disposable only)
        run: node tests/database/team-flow-concurrency.mjs

      - name: HTTP export and real SDK transport integration (disposable only)
        run: |
          supabase status --help
          npm ci --ignore-scripts
          npm install --prefix /tmp/count-stock-browser --no-save --ignore-scripts playwright@1.58.2
          /tmp/count-stock-browser/node_modules/.bin/playwright install --with-deps chromium
          node tests/http/inventory-export.mjs
          node tests/http/team-finish.mjs

      - name: Stop disposable Supabase
        if: always()
        run: supabase stop
